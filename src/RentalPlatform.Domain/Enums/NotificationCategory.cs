namespace RentalPlatform.Domain.Enums;

// Coarse grouping used for filtering / future per-category preferences.
public enum NotificationCategory
{
    Booking = 0,
    Listing = 1,
    Reminder = 2,
    Review = 3,
    Message = 4
}
