namespace RentalPlatform.Domain.Enums;

// The domain object a notification refers to (and deep-links into).
public enum NotificationEntityType
{
    Booking = 0,
    Listing = 1,
    Message = 2,
    Review = 3
}
